---
import Button from '../ui/Button.astro';
---

<section class="pt-16 pb-20 md:pt-24 md:pb-28">
  <div class="mx-auto max-w-[var(--space-container)] px-[var(--space-gutter)]">
    <div class="max-w-3xl">
      <!-- Tagline badge -->
      <p class="text-accent font-medium text-sm mb-4">
        Design Leader · Product Strategist · Builder
      </p>

      <!-- Main headline -->
      <h1 class="text-[length:var(--text-hero)] font-semibold leading-[1.1] tracking-tight text-ink mb-6">
        Je transforme la complexité en produits qui livrent.
      </h1>

      <!-- Subheadline -->
      <p class="text-lg md:text-xl text-mist leading-relaxed mb-8 max-w-2xl">
        Du cadrage à la mise en production — B2B SaaS, transformation digitale,
        et projets complexes. +15 ans d'expérience à convertir les visions en résultats mesurables.
      </p>

      <!-- CTA buttons -->
      <div class="flex flex-wrap gap-4">
        <Button href="/work" variant="primary" size="lg">
          Voir mes réalisations
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </Button>
        <Button href="/contact" variant="outline" size="lg">
          Discuter d'un projet
        </Button>
      </div>
    </div>
  </div>
</section>
